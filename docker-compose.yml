services:
  mongodb:
    image: mongo:7
    container_name: lg-industrial-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: lg_industrial
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
